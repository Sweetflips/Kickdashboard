[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]
cacheDirectories = []

[start]
cmd = "node scripts/start.js"

[providers]
node = "20"
