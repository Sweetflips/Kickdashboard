# Point Worker - Nixpacks configuration
# Self-contained worker directory with all necessary files

providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci", "npx prisma generate"]

# No Next.js build needed for worker
[phases.build]
cmds = ["echo '⚡ Worker mode - no build needed'"]

[start]
cmd = "node scripts/start-worker.js"
